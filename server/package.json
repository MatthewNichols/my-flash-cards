{
  "name": "flash-cards-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute flash-cards-db --local --file=./migrations/schema.sql",
    "db:seed": "wrangler d1 execute flash-cards-db --local --file=./migrations/seed.sql"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.4",
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241231.0",
    "wrangler": "^3.102.0",
    "drizzle-kit": "^0.30.1"
  }
}
